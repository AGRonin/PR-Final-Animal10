此文件夹中，18/50代表使用ResNet18/ResNet50作为模型。

“无”代表不使用预训练参数。

全图/200图代表是否进行子采样（即，全图使用所有Animal-10数据集数据训练，200图要求每类参与训练的上限为200张）。

加权代表使用1/c对loss加权，ln权代表使用1/lnc对loss加权，c为类别内张数。

不降低学习率代表不降低学习率，否则降低，降低情况与其训练次数有关。